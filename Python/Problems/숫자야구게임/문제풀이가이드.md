# 구현해야 되는 순서

1. `0`과 `9` 사이의 서로 다른 세 개의 숫자를 랜덤한 순서로 뽑아내는 `generate_numbers()` 함수 추가

2. 숫자 세 개를 하나씩 차례대로 입력받는 `take_guess()` 함수 추가
- 범위를 벗어나면 다시 입력 안내
- 리스트 안에 이미 같은 숫자가 있으면 다시 입력 안내

3. 스트라이크와 볼을 카운트하는 `get_score(guesses, solutions)`함수 추가

4. 1, 2, 3번을 합쳐 기능 완성