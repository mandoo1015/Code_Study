## Git의 3가지 작업 영역
Git은 내부적으로 3가지 영역을 두고 작동한다.

| 작업 영역 | 설명 |
| :--- | ---- |
| Working Directory(Working Tree) | 실제로 작업하는 프로젝트 폴더 |
| Staging Area(Index) | 커밋에 반영할 파일들을 올려두는 공간 |
| Repository | 커밋들이 저장되는 공간(.git 폴더) |

### 왜 Staging Area가 필요할까?
이게 핵심이다.

예를 들어 A.txt와 B.txt 두 파일을 수정했다고 치자. 그런데 B.txt의 변경사항만 커밋하고 싶을 수 있다. A.txt는 아직 작업 중이라서 커밋에 포함시키고 싶지 않은 경우 말이다.

**staging area가 없다면?**
-> 수정한 파일 전부가 커밋에 들어가버린다. 선택의 여지가 없다.

**staging area가 있으면?**
-> 'git add B.txt'만 실행해서 B.txt만 staging area에 올리고, 그 상태로 커밋하면 된다. A.txt는 수정된 상태로 working directory에 남아있다.

```bash
# B.txt만 staging area에 올리기
git add B.txt

# 이 상태로 커밋하면 B.txt의 변경사항만 반영됨
git commit -m "B.txt 수정"
```

💡Tip : 실제로 프로젝트를 하다 보면 "이 기능은 커밋하고 싶은데, 저 기능은 아직 테스트 중"인 상황이 자주 생긴다. staging area 덕분에 이런 세밀한 버전 관리가 가능하다.

---

## 파일의 4가지 상태
Git이 관리하는 파일은 항상 다음 4가지 상태 중 하나에 있다.

**Untracked (추적 안 됨) :** 
Git이 전혀 관리하지 않는 상태. 파일을 새로 만들고 한 번도 git add를 안 했으면 이 상태다.
```bash
# 새 파일 생성 후 상태 확인
touch newfile.txt
git status
# Untracked files: newfile.txt
```
**Tracked 상태 (3가지로 세분화)**
한번이라도 git add를 했으면 Git이 추적하기 시작한다. Tracked 상태는 다시 3가지로 나뉜다.

**1) Staged(스테이징됨)**
git add로 staging area에 올라간 상태. 다음 커밋에 포함될 예정이다.

**2) Unmodified(수정 안 됨)**
최신 커밋과 비교했을 때 변경사항이 없는 상태. 커밋 직후에는 모든 파일이 이 상태가 된다.

**3) Modified (수정됨)**
파일을 수정했지만 아직 git add를 안 한 상태

**상태 전환 흐름**
![](https://velog.velcdn.com/images/minseo_dev/post/e6ce087b-b49b-4ed4-a1e7-16e833fbb96f/image.png)
⚠️ 주의 : git status를 자주 확인하는 습관을 들이자. 현재 파일들이 어떤 상태인지 파악하는 게 중요하다.

---

## 자주 쓰는 Git 명령어 정리
**기본 설정**
```bash
# 프로젝트 폴더에서 Git 시작
git init

# 사용자 정보 설정 (커밋에 기록됨)
git config user.name "내이름"
git config user.email "이메일@example.com"
```

**파일 관리**
```bash
# 특정 파일 staging area에 올리기
git add 파일명.txt

# 특정 폴더 내 모든 변경사항 올리기
git add 폴더명/

# 모든 변경사항 올리기 (자주 씀)
git add .

# staging area에서 내리기
git reset 파일명.txt

# 현재 상태 확인
git status
```

**커밋**
```bash
# 커밋하기
git commit -m "커밋 메시지"

# 최신 커밋 수정하기 (메시지 잘못 썼을 때 유용)
git commit --amend
```

**히스토리 확인**
```bash
# 커밋 기록 보기
git log

# 한 줄로 간단히 보기
git log --pretty=oneline

# 특정 커밋의 변경사항 보기
git show 커밋아이디

# 두 커밋 간 차이 비교
git diff 커밋A아이디 커밋B아이디
```

**GitHub 연동**
```bash
# 처음 push할 때
git push -u origin master

# 이후 push
git push

# 원격 저장소 내용 가져오기
git pull

# GitHub 프로젝트 복제하기
git clone 프로젝트주소
```

**기타**
```bash
# 특정 커밋으로 되돌리기
git reset [옵션] 커밋아이디
# --soft: HEAD만 이동
# --mixed: HEAD + staging area 리셋 (기본값)
# --hard: HEAD + staging area + working directory 전부 리셋

# 커밋에 태그 붙이기
git tag 태그이름 커밋아이디

# 긴 명령어에 별명 붙이기
git config alias.별명 "원래명령어"
# git config alias.history "log pretty=oneline"
```

## git reset 자세히 알아보기
git reset에는 --soft, --mixed, --hard 세 가지 옵션이 있다.

| 유형 | 내용 |
| :--- | :---- |
| --soft | repository(HEAD)만 건드림 |
| --mixed | repository + staging area |
| --hard | repository + staging area + working directory |

그림으로 살펴보면 다음과 같다.

**현재 git의 상황 :**
![](https://velog.velcdn.com/images/minseo_dev/post/bb1653ad-bdbd-4425-a4de-e0e7054aa8ec/image.png)

**git reset --soft abab를 입력했을 때 :**
![](https://velog.velcdn.com/images/minseo_dev/post/ab8854f2-483b-4153-8a0a-2dd5c65b8f10/image.png)

**git reset --mixed abab를 입력했을 때 :**
![](https://velog.velcdn.com/images/minseo_dev/post/287e4bb1-68be-43f0-9de8-f7c4cfb0d280/image.png)

**git reset --hard abab를 입력했을 때 :**
![](https://velog.velcdn.com/images/minseo_dev/post/fccfb81a-e842-4224-af32-8812bda1e3b7/image.png)

💡Tip : --hard는 정말 조심해서 써야 한다. working directory까지 리셋되면 수정한 코드가 다 날아간다.






